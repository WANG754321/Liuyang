# 小区租房系统 — 项目汇总文档

## 一、项目概述

**项目名称**：小区租房系统（Rental System）

**项目目录**：`Lunwen/`

本项目是一个面向小区居民的在线租房平台，支持租客浏览、搜索房源并在线预约，支持房东发布和管理房源，并支持管理员对整体数据进行管理。系统采用前后端分离架构：

| 端       | 技术栈                                    | 运行端口 |
|----------|-------------------------------------------|--------|
| 后端     | Spring Boot 2.7 + MyBatis-Plus + MySQL    | 8080   |
| 前端     | Vue 3 + Vue Router + Element Plus + Vite  | 3000   |

---

## 二、系统角色

| 角色   | 标识       | 说明                         |
|--------|------------|------------------------------|
| 管理员 | `admin`    | 可查看所有订单，不可预约房源 |
| 房东   | `landlord` | 可发布、编辑、删除自己的房源；可查看名下订单 |
| 租客   | `tenant`   | 可浏览房源、预约看房、查看自己的订单 |

---

## 三、数据库设计

数据库名：`rental_db`，初始化脚本：`Lunwen/rental-backend/sql/init.sql`

### 3.1 用户表（`user`）

| 字段          | 类型           | 说明                              |
|---------------|----------------|-----------------------------------|
| `id`          | BIGINT PK AI   | 用户ID                            |
| `username`    | VARCHAR(50)    | 用户名（唯一）                    |
| `password`    | VARCHAR(100)   | 密码                              |
| `real_name`   | VARCHAR(100)   | 真实姓名                          |
| `phone`       | VARCHAR(20)    | 手机号                            |
| `email`       | VARCHAR(100)   | 邮箱                              |
| `role`        | VARCHAR(20)    | 角色：admin / landlord / tenant   |
| `status`      | TINYINT        | 状态：0=禁用，1=启用              |
| `create_time` | DATETIME       | 创建时间                          |
| `update_time` | DATETIME       | 更新时间                          |

### 3.2 房屋表（`house`）

| 字段          | 类型            | 说明                              |
|---------------|-----------------|-----------------------------------|
| `id`          | BIGINT PK AI    | 房屋ID                            |
| `title`       | VARCHAR(100)    | 房屋标题                          |
| `address`     | VARCHAR(200)    | 详细地址                          |
| `area`        | DECIMAL(10,2)   | 面积（㎡）                        |
| `price`       | DECIMAL(10,2)   | 月租金（元）                      |
| `room_count`  | INT             | 房间数                            |
| `hall_count`  | INT             | 客厅数                            |
| `floor`       | VARCHAR(20)     | 楼层描述                          |
| `orientation` | VARCHAR(20)     | 朝向                              |
| `decoration`  | VARCHAR(50)     | 装修情况                          |
| `facilities`  | VARCHAR(500)    | 配套设施（逗号分隔）              |
| `description` | TEXT            | 房屋描述                          |
| `image_url`   | VARCHAR(500)    | 房屋图片URL                       |
| `landlord_id` | BIGINT FK       | 所属房东ID                        |
| `status`      | TINYINT         | 状态：0=下架，1=上架，2=已租      |
| `create_time` | DATETIME        | 创建时间                          |
| `update_time` | DATETIME        | 更新时间                          |

### 3.3 租赁订单表（`rental_order`）

| 字段           | 类型           | 说明                                           |
|----------------|----------------|------------------------------------------------|
| `id`           | BIGINT PK AI   | 订单ID                                         |
| `order_no`     | VARCHAR(50)    | 订单编号（唯一，格式：RO+时间戳+4位随机数）    |
| `house_id`     | BIGINT FK      | 关联房屋ID                                     |
| `tenant_id`    | BIGINT FK      | 租客ID                                         |
| `landlord_id`  | BIGINT FK      | 房东ID                                         |
| `start_date`   | DATE           | 起租日期                                       |
| `end_date`     | DATE           | 结束日期                                       |
| `monthly_rent` | DECIMAL(10,2)  | 月租金                                         |
| `deposit`      | DECIMAL(10,2)  | 押金（等于一个月租金）                         |
| `total_amount` | DECIMAL(10,2)  | 总金额（月租 × 月数 + 押金）                   |
| `status`       | TINYINT        | 0=待确认，1=已确认，2=进行中，3=已完成，4=已取消 |
| `create_time`  | DATETIME       | 创建时间                                       |
| `update_time`  | DATETIME       | 更新时间                                       |

---

## 四、后端模块（`rental-backend`）

### 4.1 技术依赖

| 依赖              | 版本   | 用途                   |
|-------------------|--------|------------------------|
| Spring Boot       | 2.7.18 | 框架核心               |
| MyBatis-Plus      | 3.5.3  | ORM 框架               |
| MySQL Connector/J | 8.0.33 | 数据库驱动             |
| Lombok            | 最新   | 简化 Java 实体类代码   |

### 4.2 目录结构

```
rental-backend/src/main/java/com/rental/
├── RentalApplication.java      # Spring Boot 启动入口
├── common/
│   └── Result.java             # 统一响应封装
├── entity/
│   ├── User.java               # 用户实体
│   ├── House.java              # 房屋实体
│   └── RentalOrder.java        # 租赁订单实体
├── mapper/
│   ├── UserMapper.java         # 用户数据访问接口
│   ├── HouseMapper.java        # 房屋数据访问接口
│   └── RentalOrderMapper.java  # 订单数据访问接口
├── service/
│   ├── UserService.java
│   ├── HouseService.java
│   ├── RentalOrderService.java
│   └── impl/
│       ├── UserServiceImpl.java
│       ├── HouseServiceImpl.java
│       └── RentalOrderServiceImpl.java
└── controller/
    ├── UserController.java     # 用户相关 REST 接口
    ├── HouseController.java    # 房屋相关 REST 接口
    └── RentalOrderController.java  # 订单相关 REST 接口
```

### 4.3 公共模块（`common`）

#### `Result<T>`
统一 HTTP 响应体，包含以下字段：

| 字段      | 类型    | 说明         |
|-----------|---------|--------------|
| `code`    | Integer | 状态码（200=成功，500=失败） |
| `message` | String  | 提示信息     |
| `data`    | T       | 业务数据     |

提供静态工厂方法：`success()`、`success(T data)`、`success(String msg, T data)`、`error(String msg)`、`error(Integer code, String msg)`。

---

### 4.4 用户模块（User）

**接口路径前缀**：`/api/user`

| 方法   | 路径          | 功能           | 说明                               |
|--------|---------------|----------------|------------------------------------|
| POST   | `/login`      | 用户登录       | 匹配用户名+密码，返回用户对象      |
| POST   | `/register`   | 用户注册       | 默认注册为 `tenant`（租客）角色    |
| GET    | `/{id}`       | 查询用户信息   | 返回时屏蔽密码字段                 |
| PUT    | `/`           | 更新用户信息   | 可更新真实姓名、手机号、邮箱、密码 |

**业务逻辑**：
- 注册时检查用户名唯一性，新用户默认角色为 `tenant`，状态为 `1`（启用）。
- 登录通过用户名+明文密码比对（注：当前未做加密，生产环境应使用加密存储）。

---

### 4.5 房屋模块（House）

**接口路径前缀**：`/api/house`

| 方法   | 路径                     | 功能                 | 说明                           |
|--------|--------------------------|----------------------|--------------------------------|
| GET    | `/list`                  | 获取全部在租房源列表 | 仅返回 `status=1`（上架）的房源 |
| GET    | `/page`                  | 分页获取房源列表     | 参数：`current`、`size`        |
| GET    | `/{id}`                  | 获取单个房屋详情     |                                |
| GET    | `/search?keyword=`       | 关键字搜索房源       | 匹配标题或地址，仅返回上架房源 |
| GET    | `/landlord/{landlordId}` | 获取房东名下所有房源 | 包含所有状态                   |
| POST   | `/`                      | 发布房源             | 默认 `status=1`（上架）        |
| PUT    | `/`                      | 更新房源信息         |                                |
| DELETE | `/{id}`                  | 下架/删除房源        | 逻辑删除，设置 `status=0`      |

**状态流转**：

```
上架(1) ──租出──► 已租(2)
上架(1) ──删除──► 下架(0)
已租(2) ──取消订单──► 上架(1)
```

---

### 4.6 订单模块（RentalOrder）

**接口路径前缀**：`/api/order`

| 方法 | 路径                       | 功能             | 说明                                                   |
|------|----------------------------|------------------|--------------------------------------------------------|
| GET  | `/list`                    | 获取所有订单     | 管理员使用                                             |
| GET  | `/page`                    | 分页获取订单     |                                                        |
| GET  | `/{id}`                    | 获取单个订单     |                                                        |
| GET  | `/tenant/{tenantId}`       | 获取租客订单列表 |                                                        |
| GET  | `/landlord/{landlordId}`   | 获取房东订单列表 |                                                        |
| POST | `/`                        | 创建预约订单     | 自动计算总金额，更新房屋状态为"已租(2)"                |
| PUT  | `/confirm/{id}`            | 确认订单         | 将订单状态改为 `1`（已确认）                           |
| PUT  | `/cancel/{id}`             | 取消订单         | 将订单状态改为 `4`（已取消），并将房屋恢复为上架状态   |

**创建订单自动计算规则**：
- `monthlyRent` = 房屋月租金
- `deposit` = 房屋月租金（押一付月）
- `totalAmount` = 月租金 × 租住月数 + 押金
- 若计算月数 ≤ 0，按 1 个月计算

**订单状态流转**：

```
创建订单 ──► 待确认(0)
待确认(0) ──确认──► 已确认(1)
已确认(1) ──► 进行中(2) ──► 已完成(3)
待确认(0)/已确认(1) ──取消──► 已取消(4)
```

---

## 五、前端模块（`rental-frontend`）

### 5.1 技术依赖

| 依赖          | 版本    | 用途                     |
|---------------|---------|--------------------------|
| Vue           | 3.2+    | 前端框架                 |
| Vue Router    | 4.0+    | 客户端路由               |
| Element Plus  | 2.2+    | UI 组件库                |
| Axios         | 0.27+   | HTTP 请求                |
| Vite          | 2.9+    | 构建工具/开发服务器      |

### 5.2 目录结构

```
rental-frontend/src/
├── main.js          # 应用入口，注册全局插件
├── App.vue          # 根组件（导航栏 + 路由出口）
├── router/
│   └── index.js    # 路由配置及导航守卫
├── utils/
│   └── request.js  # Axios 封装（统一错误处理）
└── views/
    ├── Login.vue       # 登录页
    ├── Register.vue    # 注册页
    ├── Home.vue        # 首页（房源列表）
    ├── HouseDetail.vue # 房屋详情页
    ├── MyHouses.vue    # 我的房源（房东）
    ├── MyOrders.vue    # 我的订单（租客/房东）
    └── Profile.vue     # 个人中心
```

### 5.3 路由配置

| 路径            | 组件           | 访问权限                          |
|-----------------|----------------|-----------------------------------|
| `/`             | —              | 重定向到 `/home`                  |
| `/login`        | Login          | 公开                              |
| `/register`     | Register       | 公开                              |
| `/home`         | Home           | 公开                              |
| `/house/:id`    | HouseDetail    | 公开（预约操作需登录）            |
| `/my-houses`    | MyHouses       | 需登录，仅 `landlord` / `admin`   |
| `/my-orders`    | MyOrders       | 需登录，所有角色                  |
| `/profile`      | Profile        | 需登录，所有角色                  |

导航守卫逻辑：未登录访问需认证页面时跳转至登录页，并记录 `redirect` 参数；角色不符时跳转至首页。

### 5.4 工具模块（`utils/request.js`）

对 Axios 进行统一封装：
- `baseURL` 设为 `/api`，通过 Vite 代理转发至 `http://localhost:8080`
- 请求超时：10 秒
- 响应拦截：业务 `code !== 200` 时弹出 `ElMessage.error` 并返回拒绝的 Promise

### 5.5 根组件（`App.vue`）

全局导航栏功能：
- 显示系统名称"小区租房系统"，点击跳转首页
- 已登录用户：展示头像、昵称、角色标签和下拉菜单（个人中心 / 我的订单 / 我的房源 / 退出登录）
- 未登录状态：展示"登录"和"注册"按钮
- 监听路由变化，实时更新用户状态

---

### 5.6 页面功能说明

#### 登录页（`Login.vue`）

- 表单校验：用户名、密码均为必填
- 调用 `POST /api/user/login` 接口，登录成功后将用户信息存入 `localStorage`
- 登录成功后触发 `login-success` 事件（用于更新父组件导航栏）并跳转至目标页面
- 页面展示内置测试账号（管理员 / 房东 / 租客）

#### 注册页（`Register.vue`）

- 新用户注册，调用 `POST /api/user/register`，角色默认为 `tenant`

#### 首页（`Home.vue`）

- 展示所有上架（`status=1`）房源的卡片列表（3列网格布局）
- 每张卡片显示：房屋图片（无图片时展示图标）、标题、地址、月租金、户型和面积
- 支持关键字搜索（按标题或地址模糊匹配）
- 已登录用户可快速跳转至"我的房源"或"我的订单"
- 未登录用户提示引导登录/注册

#### 房屋详情页（`HouseDetail.vue`）

- 路由参数：`/house/:id`
- 左侧展示房屋大图，右侧展示详细属性：价格、地址、面积、户型、楼层、朝向、装修、配套设施、描述
- 房屋状态标签：可租（绿色）/ 已租（橙色）/ 下架（灰色）
- 根据用户角色决定操作区：
  - 未登录：展示提示，点击跳转登录
  - 租客（`tenant`）：展示"立即预约"按钮，弹出预约对话框（选择起租/结束日期）
  - 房东（`landlord`）：提示无法预约
  - 管理员（`admin`）：提示无法预约

#### 我的房源（`MyHouses.vue`）——房东专用

- 以表格形式展示当前房东所有房源（含下架、已租状态）
- 支持**添加**房源（填写标题、地址、租金、面积、户型、楼层、朝向、装修、配套、描述）
- 支持**编辑**已有房源信息
- 支持**删除**房源（逻辑删除，设置 `status=0`）
- 调用接口：`GET /api/house/landlord/{landlordId}`、`POST /api/house`、`PUT /api/house`、`DELETE /api/house/{id}`

#### 我的订单（`MyOrders.vue`）

- 展示订单列表，包含：订单编号、房屋名称、起租日期、结束日期、总金额、状态
- **租客**：显示自己发起的订单；可确认（`status=0` → `1`）、取消（`status<2`）
- **房东**：显示名下房源的所有租赁订单；可确认或取消
- **管理员**：显示全部订单
- 状态颜色：待确认（橙）/ 已确认（蓝）/ 进行中（绿）/ 已完成（灰）/ 已取消（红）

#### 个人中心（`Profile.vue`）

分为三个标签页：

1. **个人信息**：修改真实姓名、手机号、邮箱（用户名不可修改）
2. **修改密码**：填写当前密码、新密码、确认新密码（含一致性校验）
3. **我的订单**（仅租客可见）/ **我的房源**（仅房东可见）：以表格形式快速预览

左侧卡片展示：头像、昵称、角色标签，以及收藏房源数或预约订单数统计。

---

## 六、快速启动指南

### 前置要求

- JDK 8+
- Maven 3.6+
- Node.js 16+
- MySQL 8.0+

### 步骤

1. **初始化数据库**

   ```bash
   mysql -u root -p < Lunwen/rental-backend/sql/init.sql
   ```

   或在 Windows 上运行 `Lunwen/rental-backend/init-db.bat`。

2. **配置数据库连接**

   编辑 `Lunwen/rental-backend/src/main/resources/application.yml`，修改 `username` 和 `password`：

   ```yaml
   spring:
     datasource:
       url: jdbc:mysql://localhost:3306/rental_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
       username: root
       password: 你的密码
   ```

3. **启动后端**

   ```bash
   cd Lunwen/rental-backend
   mvn spring-boot:run
   ```

   或在 Windows 上运行 `Lunwen/start.bat`（会同时启动前后端）。

4. **启动前端**

   ```bash
   cd Lunwen/rental-frontend
   npm install
   npm run dev
   ```

5. **访问系统**

   打开浏览器访问：[http://localhost:3000](http://localhost:3000)

### 内置测试账号

| 账号       | 密码     | 角色   |
|------------|----------|--------|
| admin      | admin123 | 管理员 |
| landlord1  | 123456   | 房东   |
| tenant1    | 123456   | 租客   |

---

## 七、系统架构图

```
┌─────────────────────────────────────┐
│          浏览器（前端）              │
│  Vue 3 + Element Plus + Vue Router  │
│  端口: 3000                          │
└──────────────────┬──────────────────┘
                   │  HTTP /api/** (Vite Proxy)
                   ▼
┌─────────────────────────────────────┐
│          后端（Spring Boot）         │
│  Controller → Service → Mapper      │
│  端口: 8080                          │
└──────────────────┬──────────────────┘
                   │  JDBC (MyBatis-Plus)
                   ▼
┌─────────────────────────────────────┐
│          MySQL 数据库                │
│  rental_db                          │
│  user | house | rental_order        │
└─────────────────────────────────────┘
```
